{% if section.text.prefix %}
  <p class="prefix">{{ section.text.prefix }}</p>
{% endif %}

{% if section.text.title %}
  {% if section.text.header === "h1" %}
    <h1 class="title">{{ section.text.title }}</h1>
  {% elif section.text.header === "h2" %}
    <h2 class="title">{{ section.text.title }}</h2>
  {% else %}
    <h3 class="title">{{ section.text.title }}</h3>
  {% endif %}
{% endif %}

{% if section.text.subtitle %}
  <p class="sub-title">{{ section.text.subtitle }}</p>
{% endif %}

{% if section.text.prose %}
  <div class="prose">{{ section.text.prose | mdToHTML | safe }}</div>
{% endif %}